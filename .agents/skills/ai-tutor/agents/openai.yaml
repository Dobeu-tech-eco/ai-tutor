interface:
  display_name: "Ai Tutor"
  short_description: "Repo-specific patterns and workflows for ai-tutor"
  default_prompt: "Use the ai-tutor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true